// Shapeless / Mutable object (JS like).
// Use only when you know what you are doing (Kinda defeats the purpose of TS, like :any).
// Avoid if possible.
type AnyObject={[key:string]:any}

export default AnyObject
